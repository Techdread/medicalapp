# Example .env file for the backend
# Copy this to .env in the 'backend' directory and fill in values if needed.

# Hugging Face Model ID for MedGemma (default is 'google/medgemma-4b-it' in services.py)
# MODEL_ID=google/medgemma-4b-it

# Optional: Specify a specific GPU if 'device_map="auto"' isn't desired (e.g., "cuda:0")
# CUDA_VISIBLE_DEVICES=0 # This is typically set in the environment before running the script

# Logging level for Uvicorn (e.g., info, debug, warning, error, critical)
# LOG_LEVEL=info
